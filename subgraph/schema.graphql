type Attestation @entity {
  id: ID!
  blockNumber: BigInt!
  attestor: Bytes! # address
  profile: Bytes! # address
}

type Post @entity {
  id: ID!
  blockNumber: BigInt!
  contentURI: String! 
}

type Comment @entity {
  id: ID! 
  blockNumber : BigInt!
  postID: ID!
  contentURI: String!
}